# Config

Project configuration placeholders.

- `config.yaml` — central params (data URLs, model hyperparams, plot style)
- `params.json` — alternative JSON params for Python pipelines

> TODO: Wire `config.yaml` into scripts via `yaml` / `config` packages.
