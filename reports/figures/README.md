# Report Figures — Placeholder

Copied/symlinked from `../output/` and `../figures/` on report build.

Expected files (placeholders, gitignored until build):
- `wine-1.png` … `wine-8.png` (R outputs)
- `wine_quality_counts.png` etc. (Python outputs)

> TODO: Add Makefile target `report-figures` to auto-copy.
